DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

JWT_SECRET=

# Comma-separated allowed frontend origins for CORS
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend (Next.js) — copy into frontend/.env.local as needed
NEXT_PUBLIC_API_URL=
